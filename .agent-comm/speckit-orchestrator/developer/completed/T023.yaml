task_id: T023
status: completed
files_modified: [tests/test_professor_report_data.py]
files_created: []
test_result:
  command: "pytest tests/test_professor_report_data.py -v -k at_risk"
  passed: 0
  failed: 17
  note: "RED phase confirmed — identify_at_risk() new signature with misconception_counts: dict[int, int] not implemented"
notes: >
  17 tests written for identify_at_risk() covering all 6 at-risk criteria.
  Tests target the NEW function signature:
    identify_at_risk(row, class_mean, class_std, misconception_counts: dict[int, int])
  The existing implementation uses total_misconception_count: int = 0 (old signature),
  so all 17 tests fail with:
    TypeError: identify_at_risk() got an unexpected keyword argument 'misconception_counts'
  Additionally, reason strings differ (new spec: "전체 점수 {score:.2f} (기준: 0.45 미만)",
  "Z-점수 {z:.2f} (기준: -1.0 미만)", "문항 {sn}번 커버리지 0%", "오개념 {n}개 (기준: 3개 이상, 비Advanced)",
  "평균 커버리지 {avg:.1%} (기준: 30% 미만)", "모든 문항 '하' 수준").
  The _make_at_risk_row() helper uses dicts (dict[int, str], dict[int, float]) matching
  the actual StudentSummaryRow dataclass fields.
  Tests written: 2 per criterion (positive + negative), plus OR logic, std=0 edge case,
  and two empty-collection guard tests (criterion 3 + criterion 4/6).
completed_at: "2026-03-08T00:00:00"
